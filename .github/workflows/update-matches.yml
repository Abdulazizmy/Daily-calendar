name: تحديث المباريات تلقائياً

on:
  schedule:
    - cron: "0 */3 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-matches
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: جلب الجدول والنتائج من ESPN
        run: python scripts/update_matches.py

      - name: حفظ التغييرات
        run: |
          if [ -n "$(git status --porcelain matches.json matches-meta.json)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add matches.json matches-meta.json
            git commit -m "تحديث المباريات تلقائياً"
            git push
          else
            echo "ما فيه تغييرات"
          fi
